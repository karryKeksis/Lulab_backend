type Query {
  #验证码相关
  # sms
  verifySend(mobile: String!, area: Int!): Res!
  # user
  loginCaptcha(mobile: String!, area: Int!, code: Int!): Return!
  loginOne_click(mobile: String!, accessCode: String!, outId: String!): Return!
  loginPassword(mobile: String!, password: String!): Return!
  #登陆注册
  logOut: Res!
<<<<<<< HEAD

  courseCategory: [Category!]
  course(category_id: String!, page: Int!, limit: Int!): [Course!]
  courseDetail(course_id: String!): [CourseDetail!]
=======
>>>>>>> b8101ac6c72b76a045c94d1be186d91764542866
  #课程请求
  courseCategory: [Category!]
  course(category_id: String!, page: Int, limit: Int): [Course!]
  courseDetail(course_id: String!): [CourseDetail!]
  courseCatalogue(course_id: String!): [Catalogue!]
  courseLink(detail_id: String!): Link!

  # 获取支付信息
  reToken(token: String!): Token!

  # user
  user: [User!]

  # 获取支付信息
  orderInfo(number: Int!): OrderInfo!

  # 七牛云
  qiniu: Qiniu!

  #退出登陆

  #获取商品信息
  goodInfo(number: Int!): GoodRes!

  #alipayinfo
  alipayInfo(productInput:ProductInput!):ProductInfo
  alipayCallback(queryInput:QueryInput!):result
  alipayRefund(refundInput:RefundInput!):result
  alipayOrderInfo(orderInfoInput:OrderInfoInput!):[orderInfoResult]
  alipayOrderDetail(orderDetailInput:OrderDetailInput!):orderDetailResult
}
