type Mutation {

  #adduser(username: String!): User
  
  passwordChange(mobile: String!, password: String!,code: String!): Res
  
  #alipay
  Alipaysdk(alipayInput:AlipayInput!):result

  #alipaymobile
  alipayOrder(orderInput:OrderInput!):orderResult

}